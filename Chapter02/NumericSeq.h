#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

void display(const vector<int>&, ostream& = cout);
void bubble_sort( vector<int>&, ofstream* = 0);
void swap(int &, int &);
